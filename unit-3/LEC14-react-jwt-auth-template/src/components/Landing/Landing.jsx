const Landing = () => {
    return (
        <main>
            <h1>You are on the landing page</h1>
            <p>Sign Up or Sign In to see a message</p>
        </main>
    );
};

export default Landing;